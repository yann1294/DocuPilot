{
  "name": "docupilot-serverless",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --noEmit",
    "sam:build": "sam build",
    "sam:validate": "sam validate",
    "sam:deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.820.0",
    "@aws-sdk/client-s3": "^3.820.0",
    "@aws-sdk/client-ssm": "^3.820.0",
    "@aws-sdk/lib-dynamodb": "^3.820.0",
    "@aws-sdk/s3-request-presigner": "^3.820.0",
    "@google/generative-ai": "^0.24.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/jsonwebtoken": "^9.0.6",
    "typescript": "^5.8.3"
  }
}
